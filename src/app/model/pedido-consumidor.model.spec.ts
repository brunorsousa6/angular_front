import { PedidoConsumidor } from './pedido-consumidor.model';

describe('PedidoConsumidor', () => {
  it('should create an instance', () => {
    expect(new PedidoConsumidor()).toBeTruthy();
  });
});
