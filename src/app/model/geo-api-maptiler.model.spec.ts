import { GeoApiMapTilerData } from './geo-api-maptiler.model';

describe('GeoApiMapTilerData', () => {
  it('should create an instance', () => {
    expect(new GeoApiMapTilerData()).toBeTruthy();
  });
});
